CREATE TABLE delivery_challan_master (
    id BIGINT AUTO_INCREMENT PRIMARY KEY,
    name VARCHAR(255),
    descriptions VARCHAR(255),
    status BOOLEAN,
    created_date DATETIME,
    last_modified_date DATETIME
);

CREATE TABLE delivery_challan (
    id BIGINT PRIMARY KEY,
    name VARCHAR(255),
    descriptions VARCHAR(255),
    status BOOLEAN,
    created_date DATETIME,
    last_modified_date DATETIME
);
